# FOBE — Python dependencies
#
# Install:  pip install -r requirements.txt
# Optional: pip install -r requirements-eval.txt   (for the eval pipeline)

# ── Explorer server (required) ────────────────────────────
fastapi>=0.110
uvicorn>=0.29
python-dotenv>=1.0
supabase>=2.28
PyJWT[cryptography]>=2.10
boto3>=1.34
PyMuPDF>=1.24

# ── Eval pipeline (optional — install separately) ─────────
# See requirements-eval.txt
