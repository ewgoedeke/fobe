# FOBE — Eval pipeline dependencies
#
# Install:  pip install -r requirements-eval.txt
# These are needed for running eval/run_eval.py, LLM tagging, and retraining.

# ── Core pipeline ─────────────────────────────────────────
PyYAML>=6.0
numpy>=1.26
scikit-learn>=1.4
anthropic>=0.25

# ── Document ingestion (batch_ingest.py) ──────────────────
docling>=2.0

# ── Embedding-based ranking (rank_pages_embed.py) ─────────
# These pull in PyTorch (~2GB); install only if using embedding models.
# sentence-transformers>=3.0
# lightgbm>=4.3
# torch>=2.2
